import type {Theme} from "theme-ui";

export const theme: Theme = {
  // initialColorModeName: "light",
  colors: {
    primary: "#885FFF",
    modes: {
      dark: {
        primary: "black",
      },
    },
  },
};
